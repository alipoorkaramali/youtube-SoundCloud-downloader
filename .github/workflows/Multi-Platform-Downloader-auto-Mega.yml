name: 🎬🎵 Auto Downloader + Mega.nz

on:
  workflow_dispatch:
    inputs:
      platform:
        description: '🌐 پلتفرم'
        required: true
        default: 'youtube'
        type: choice
        options:
          - youtube
          - soundcloud
      url:
        description: '🔗 لینک دانلود'
        required: true
      folder:
        description: '📁 پوشه در مخزن'
        required: false
        default: 'news_downloads'
      mega_folder:
        description: '📂 پوشه در Mega.nz'
        required: false
        default: 'YoutubeNews'

jobs:
  download:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔐 Decrypt cookies
        run: |
          if [ ! -f cookies.txt.gpg ]; then
            echo "❌ فایل cookies.txt.gpg یافت نشد!"
            exit 1
          fi
          gpg --batch --yes --passphrase "${{ secrets.COOKIE_DECRYPT_KEY }}" --decrypt cookies.txt.gpg > cookies.txt
          if [ ! -s cookies.txt ]; then
            echo "❌ رمزگشایی کوکی ناموفق بود یا فایل خالی است"
            exit 1
          fi
          echo "✅ کوکی با موفقیت رمزگشایی شد"

      - name: ⚙️ Setup yt-dlp + ffmpeg
        uses: AnimMouse/setup-yt-dlp@v3
        with:
          aria2: true
          ffmpeg: true

      - name: 📦 Install rclone
        run: |
          sudo apt-get update
          sudo apt-get install -y rclone
          rclone version

      - name: 🔓 Decrypt rclone config
        run: |
          mkdir -p ~/.config/rclone
          if [ ! -f rclone_mega.conf.gpg ]; then
            echo "❌ فایل rclone_mega.conf.gpg پیدا نشد!"
            exit 1
          fi
          gpg --quiet --batch --yes --decrypt --passphrase="${{ secrets.RCLONE_PASSPHRASE }}" \
              --output ~/.config/rclone/rclone.conf rclone_mega.conf.gpg
          chmod 600 ~/.config/rclone/rclone.conf
          echo "✅ rclone config loaded"
          # بررسی سریع (اختیاری)
          rclone listremotes || echo "⚠️ هشدار: لیست ریموت‌ها نمایش داده نشد"

      - name: ▶️ دانلود فایل
        run: |
          mkdir -p "${{ github.event.inputs.folder }}"
          cd "${{ github.event.inputs.folder }}"
          
          if [ "${{ github.event.inputs.platform }}" = "youtube" ]; then
            yt-dlp -f "bestaudio[ext=m4a]/bestaudio/best" --audio-format mp3 --audio-quality 0 \
              --cookies ../cookies.txt --no-playlist \
              "${{ github.event.inputs.url }}" \
              -o "%(title)s.%(ext)s"
          else
            # soundcloud
            yt-dlp -f "bestaudio" --audio-format mp3 --audio-quality 0 \
              --no-playlist \
              "${{ github.event.inputs.url }}" \
              -o "%(title)s.%(ext)s"
          fi
          
          echo "✅ دانلود انجام شد"
          echo "فایل‌های موجود در پوشه:"
          ls -la

      - name: ☁️ آپلود به Mega.nz
        run: |
          rclone copy "${{ github.event.inputs.folder }}" "mega:${{ github.event.inputs.mega_folder }}" \
            --progress --transfers=4 -vv
          echo "✅ فایل با موفقیت به Mega.nz آپلود شد!"

      - name: 🕒 Commit to Repository
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add "${{ github.event.inputs.folder }}"
          git commit -m "Auto download: ${{ github.event.inputs.url }}" || echo "No changes to commit"
          git push
